var CategoryController = function ($rootScope, $scope, $cookies, $http, $window, $state) {
};

CategoryController.prototype = {
  init: function () {
  }
};

CategoryController.$inject = ['$rootScope', '$scope', '$cookies', '$http', '$window', '$state'];

export default CategoryController;
